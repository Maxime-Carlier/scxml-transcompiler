package functionnal.states;

import model.AbstractState;

/**
 * Generated state for FSM ${Fsm.name}
 */
public class ${State.name} extends AbstractState{
    public ${State.name}(){
        name="${State.name}";
    }
}